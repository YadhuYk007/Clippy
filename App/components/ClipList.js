import React, {useEffect, useState} from 'react';
import {Text, View, StyleSheet} from 'react-native';

const ClipList = props => {
  console.log('From List Screen', props.cId);
  return null;
};
export default ClipList;
